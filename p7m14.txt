  <div class="mobmeta">${esc(mobDose(st))}</div>
  <div class="mobcue">${esc(t(st.cueKey))}</div>
  ${clock}
  <div class="mobnav">
   <button class="btn" style="min-height:44px;width:100%" onclick="mobNext()">${esc(t("mob_ready"))}</button>
  </div>
 </div>`;
}

function mobHtmlDone(){
 const p=mobPreset(mobLive&&mobLive.presetId);
 return `<div class="mobwrap moblive">
