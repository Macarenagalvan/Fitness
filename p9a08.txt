
rep(
"/* ===== INIT ===== */",
block + "\n\n/* ===== INIT ===== */",
"block",
)

for s in ["function logsByType","function renderResumen()","id=\"weekBody\"","id=\"progTabs\"","id=\"progRunning\"","wk_this","pg_nokm","function renderProgYoga"]:
    if s.replace('\\','') not in t and s not in t:
        # weekBody check
        pass
if "id=\"weekBody\"" not in t:
    raise SystemExit("missing weekBody")
if "function logsByType" not in t:
    raise SystemExit("missing logsByType")
if t.count("function renderProgreso")!=1:
    raise SystemExit("renderProgreso count "+str(t.count("function renderProgreso")))
print("ok sanity")
p.write_text(t)
print("wrote", p.stat().st_size)
