  {id:"sl_sava",titleKey:"mob_sava_side",cueKey:"mob_sava_side_cue",mode:"time",sec:120}
 ]}
];

let mobLive=null;
let mobTick=null;

function mobilityLogs(){ return (DB.logs||[]).filter(l=>sessionType(l)==="mobility"); }
function mobPreset(id){ return MOBILITY_PRESETS.find(p=>p.id===id)||null; }
function mobCurrent(){ if(!mobLive) return null; const p=mobPreset(mobLive.presetId); if(!p) return null; return p.steps[mobLive.step]||null; }
function mobFmt(sec){ sec=Math.max(0,sec|0); const m=(sec/60)|0,s=sec%60; return m+":"+String(s).padStart(2,"0"); }
function mobSideLabel(side){ if(side==="R") return t("mob_side_r"); if(side==="L") return t("mob_side_l"); return ""; }
