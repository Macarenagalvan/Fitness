#!/usr/bin/env python3
from pathlib import Path
p = Path("index.html")
t = p.read_text()
block = Path("week-block.js").read_text()
es = Path("i18n-es.txt").read_text().rstrip()
de = Path("i18n-de.txt").read_text().rstrip()

def rep(old, new, label):
    global t
    n = t.count(old)
    if n != 1:
        raise SystemExit(f"FAIL {label}: count={n}")
    t = t.replace(old, new)
    print("ok", label)

CSS = """
.wknav{display:flex;align-items:center;gap:8px;margin-bottom:12px}
.wkarr{flex:none;min-width:44px;min-height:44px;padding:0}
.wktitle{flex:1;min-width:0;text-align:center}
