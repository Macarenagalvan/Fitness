/* ===== FASE 8 · HOY + CARGA ===== */
const HOY_LEG_CAT={legs:1,glutes:1};
function hoyDiff(date){
 const a=new Date((date||today())+"T00:00:00");
 const b=new Date(today()+"T00:00:00");
 return Math.round((b-a)/86400000);
}
function runningLogs(){ return (DB.logs||[]).filter(l=>sessionType(l)==="running"); }
function hoyLast(arr){ return arr&&arr.length?arr[arr.length-1]:null; }
function strengthExCat(e){
 if(!e) return "";
 if(e.cat) return e.cat;
 const lib=LIB[e.libId||e.id];
 return lib?lib.cat:"";
}
function strengthLogHasLegs(log){
